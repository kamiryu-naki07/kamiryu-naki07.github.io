/*$(window).on('load resize', function(){
	if($(window).width() > 600){
		if($('body'){				
			// ワークスのオンマウス
			$('.Works a').hover(function(){
				$(this).parent().children('h2, p').stop().fadeTo(500, 1).shuffleLetters();
				},
				
			function(){
				$(this).parent().children('h2, p').stop().fadeTo(300, 0);
				});
			}
		}
	});
*/
